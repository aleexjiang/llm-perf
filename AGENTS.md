# AGENTS.md — llm-perf 工作指南

给在此仓库工作的 AI 助手。项目定位：客户自部署 LLM 推理服务的数据采集工具（Go 单二进制）。项目边界与运行方式见 `CODEBUDDY.md`，本文记录设计决策的"为什么"和工作纪律。

## 设计决策

- **工具专注数据采集，报告层剥离（2026-09-17 拍板）**：报告（gen_html_report.py 一页纸/图表/结论启发式，3000 行）随迭代不断加厚，已喧宾夺主——工具的价值在**产出的原始数据可信、完整、自描述**，不在报告好看。落点：报告管线整体移除，数据契约（`docs/data-contract.md` + 每份 JSON 的 `schema_version`）升格为对外接口，聚合/判级/呈现全部外移（pandas/notebook/BI）。配套：泊松延迟重整 + `burstiness`（跨 run 可比，方法论唯一"必抄"项）、`raw_timings` 原始 chunk 序列落盘（峰值秒桶/抖动分析在采集端留原料）。「报告侧能算的不碰 Go」改述为「分析侧能算的不碰 Go」，指标冻结纪律不变。
- **原始请求一条不丢（2026-09-17 拍板）**：warmup、benchmark、correctness、失败和主动取消都采集完整 `TurnMetrics`；用途通过 `phase` 标记，warmup/correctness 放 `auxiliary_requests[]`，不进入 benchmark KPI；失败请求进入失败计数，主动取消不进入完成数/SLO 分母/主吞吐。采集完整与统计隔离是两条独立规则，不能用「不计统计」作为丢弃数据的理由。
- **测量哲学：受控变量优先**。定位是测量工具——每个维度可隔离、可归因；真实感来自测试设计（配置组合），不来自把真实因素揉进一次跑分。
- **工具的最终目的是保证「agent 产品接入的模型服务」能很好支撑用户体验（2026-09-16 拍板）**：判据以终端用户体验口径为先——单流速度、用户可感知的 TTFT/排队、容量拐点（多少并发下体验还撑得住）；服务端聚合吞吐与资源指标只作归因诊断（解释「为什么」），不充当体验判据。12.10 熔断改单流是同一原则的直接落地。
- **轮间思考时间（think-time）不做**。TTFT/TPOT 是 per-request 指标，用户思考间隔不影响用户体验；到达节奏用开环 `request_rate`（Poisson）控制，比模拟用户发呆更直接、更可复现。
- **起点分散用"错峰启动"实现**。开环 multiturn（`request_rate` 在 multiturn 语义下 = 会话到达率）已天然支持会话陆续启动、各自增长；不采用开局注入历史的方式。
- **RPS 是默认主路径，闭环并发仅作辅助诊断（2026-09-17 拍板）**：开环 `request_rate/rate_sweep` 表示会话到达率；闭环 `levels` 统一 barrier 齐射，不再提供 `ramp/ramp_factor`，避免两套加压节奏和失败止损语义混入采集器。服务端失败、取消和 drain 数据都继续完整落盘。
- **冷、暖 prefill 分开测、分开标注**。真实 agent 长上下文请求多为暖前缀（prefix cache 命中，TTFT 只由新增 token 决定）；冷 156K 只出现在首请求注入巨量上下文或缓存失效，是少数路径，且冷、暖 TTFT 差一个数量级，混报无法解读。冷路径 ≡ single 大 prompt + 换 seed 击败缓存，无需新代码；冷多轮会让 prefill 总量随轮数二次方膨胀，标称并发与真实负载不对等。
- **TTFT 体验判据以 30–40K 输入档为准**（agent 场景）。现代 agent 产品基线上下文即 ~35K（系统提示 + 工具定义 + RAG 注入，用户发一句"你好"请求就带 35K 上下文），短输入档徽章代表不了 agent 体验。报告基线用 3 档制：优/及格（≤4K，MLPerf Interactive/Server 直引）+ agent 大上下文档（30–40K，推导值），判据与出处见 `docs/latency-baselines.md`。
- **tool-call 只落 `bench probe` 健康检查**（默认开），不进压测路径。客户业务若 agent 主导，纯推理压测的吞吐会偏乐观，对外汇报需主动说明此边界。
- **multiturn 起步上下文按 agent 真实形状设定（~35K）**。filler 模式 turn1 只有 15.5k（system 基座 3.2k），测不出大 prefill 冷启动，而首字延迟正是 agent 产品的真实痛点——起步值必须与 30–40K 体验判据同一量级。35K 是**推导值待取证**（AgentLens 按 session 首调 inputTokens 分布回填），先用配置实现、不等数据。形状取"起步 35K + 每轮 10K × 8 轮"（末端 ~108K）：保 8 轮历史深度优先于保单轮增量——多轮衰减曲线的横轴长度比每轮增量大小更关键；起步抬高后原 12.3K 增量会把末端推到 125K+，撞 128K 上限并触发"截断式跳过剩余 session"。
- **配置 schema 不保向后兼容**：迭代期改字段直接改类型，不做双轨兼容。
- **告警必须附可核实证据**（请求摘要 + 响应片段 + capture 路径），判据保守化（不确定降 WARN），capture 显式指定目录才落盘。
- **指标层冻结 + 加法准入门槛（2026-09-12 拍板）**：评测指标只有四个数——TTFT、decode 速度（TPOT/聚合 tok/s，ITL 方差并入）、goodput@SLO、正确性 canary；`TurnMetrics` 核心字段封版，新增字段必须指认四数之一，否则 `json:"-"` 只进诊断面。新功能立项必须指认服务"控制层（把系统放到工作点）或诊断层（数坏了往哪查）"，指认不出不做；配置面不开新顶层旋钮（新能力优先预设/文档指南），报告做减法（一页纸）。体系论证见 `docs/testing-architecture.md`。
- **trace 与 filler 分工；多模型隔离优先（2026-09-12 拍板）**：trace 是负载层第一公民，第一交付物是离线画像（把客户流量翻译成 filler 格子参数），trace 线整体暂缓；filler 是受控变量仪器，不因 trace 降级。多模型（单端点、各模型独占 GPU）默认路径 = 逐模型隔离测试 + 报告侧组合归因；混合回放为条件触发（复活条件：组合预测与客户线上体感失配且排除客户端/网络因素）——交互效应弱时叠加预测成立，不预支测二阶效应。
- **同一目标、多条路径必须同口径（2026-09-12）**：filler 的合成词表与语料两条生成路径各用一套长度换算（「词/token」vs「chars/token」），同档位负载差 2.49×（实测 500tk 档实发 1246 token）——在受控变量实验里，「换数据源」变成了「换负载量级」，格子不可比。两路已统一到 `corpus.CharsPerToken`（en 4.0 / zh 1.4，真机校准偏差 <2%），并由 probe `filler_fidelity` 实测本部署真实换算比、偏离 >25% 告警。**凡「同一目标、多条生成/解析路径」处，先对齐口径再谈对比**；口径系数一律做成可实测（而非只写注释）——注释不会随 tokenizer 变化而报警。
- **客户端计时是测量点选择，不是缺陷（2026-09-12 拍板）**：归因"产品慢 vs 服务慢"必须在客户端测，网络路径是特性不是误差。工具开销（µs 级/chunk）与被测间隔（18ms+）差 3~4 个数量级，危险形态仅"数百并发流+高 chunk 率客户端成瓶颈"；守卫靠两源一致性检查（客户端聚合吞吐 vs smetrics 服务端吞吐，偏差超阈值报告标注），不做计时架构重构。
- **测试类别（`test:`）只切表达层，不切测量（2026-09-13）**：`benchmark|performance|soak` 是同一套数据（四个数 + 三分归因）的三种表达焦点——报告按类别换首屏口径，**绝不为类别复制管线或另立判据**（判据仍只有一份 `slo.baseline`）。跨部署可比的前提是「同一组格 + 同一 filler 口径 + 同一判据」，三者任一被动则数不可比：故基准预设 `configs/benchmark.yaml` 刻意**不写** `slo.baseline`（各部署各写一把尺子＝横比失效），也不含探索性内容（拐点/饱和点/混跑/输出长度扫描属 performance）。取不到的证据（如 soak 的分时段漂移）在报告里如实写 NA，不为了"看起来完整"编趋势。
- **混合档取跨会话混合（2026-09-16 拍板）**：并发多轮的增量档差按**会话**分派——每会话属一档、按权重 SWRR 确定性分配（`multiturn.profiles`），不做会话内逐轮换档：真实"重会话/轻会话"是不同用户轨迹之差，会话内突变无对应真实形态，且破坏逐轮斜率可读性。会话档位标签 `profile` 随 MultiturnRun 落盘，供报告侧分档切体验。【已实现】

## 负载形态矩阵（对外汇报口径）

| 路径 | 场景 | 状态 |
|---|---|---|
| 暖（真实 agent 典型体验） | 错峰开环 multiturn，逐轮共享前缀 | 已有能力 |
| 冷（最坏角落） | single 大 prompt + 换 seed 击败缓存 | 已有能力 |
| 缓存收益量化 | `fixed_seed` true/false 对照 + server_metrics 命中率 | 已有能力 |
| 稳态 soak | 会话续跑（`duration_seconds` + `renew`） | 已有能力 |

## 工作纪律

- **实测配置不入库**（2026-09-11 拍板）：客户环境配置 `configs/customer.yaml`（gitignore 兜底）、
  自有环境配置放本地测试目录（当前 `llm-perf-test/configs/`，整目录 gitignore 不入库）——仓库只保留模板
  （`example.yaml`）与 smoke 系列，端点/key/场景参数一律不进 git。
- 项目边界与运行方式沉淀到 `CODEBUDDY.md`；重要设计拍板沉淀到本文件，操作口径沉淀到 `docs/`。
- 验证基线：`gofmt -l internal/ cmd/` 必须无输出（连续多轮提交遗漏 gofmt 格式，已成惯性问题——提交前必跑，新文件也不例外）+ `go build ./... && go vet ./... && go test ./... -count=1` 全绿再交付；HTML 报告改动用真实 JSON 渲染人工核对。
- CLI/config 交互逻辑改动后必跑 `scripts/smoke.sh`（BENCH 指向已有二进制免编译）：它对输出 JSON 做数据形状断言（模型×变体分布、CLI 过滤、错误路径、报告管线），`go test` 覆盖不到的端到端行为都在这里兜底。

## 工程陷阱

- 同一文件多个 Edit 并行会互相覆盖 → 串行编辑，改完 grep 复核。
- Bash `grep` 对部分模式静默返回空 → 扫代码用 Grep 工具，关键结论交叉验证。
- bench CLI：配置用 `-c`；并发场景需 `--concurrency cfg`；输出别用 `head` 截断（SIGPIPE 杀进程），重定向到文件。
- `TurnMetrics` 新增字段标 `json:"-"`，避免污染压测 JSON 契约；报告侧能算的指标不进 Go。
- 展示口径 = 执行口径：测试画像（启动总览）的档位列表与上下文 reach 必须复用 `ClampLadder` 与现有估算函数，不另写一套估算；reach 是估算值，输出带 `~`。
